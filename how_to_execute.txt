start docker daemon (Docker desktop on macOS, on Ubuntu autostarts on its own)

cd in NLPipe/src/aws/fargate

docker compose up

run sh cmd/initial_import.sh

env $(cat .env | xargs) go run .

test on browser: localhost:8001/

newman run apitest.json (-n sets number of serial repetitions)




//TODO create bash script to launch simultaneous calls
